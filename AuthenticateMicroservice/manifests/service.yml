apiVersion: v1
kind: Service
metadata:
    name: sattwikroyauthenticatemicroservice
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: sattwikroyauthenticatemicroservice